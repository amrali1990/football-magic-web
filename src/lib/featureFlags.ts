// Central feature toggles.

// Temporarily hides the account-related UI (Favorites, Settings, Login/Profile
// links in the sidebar, bottom nav, and navbar) while keeping all of its code
// in place — set to true to re-enable everywhere at once.
export const SHOW_USER_FEATURES = false;
